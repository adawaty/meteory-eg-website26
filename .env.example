# Copy to .env.local for local dev (DO NOT commit)

# Vercel serverless functions (/api)
DATABASE_URL=postgresql://neondb_owner:REPLACE_ME@YOUR-NEON-HOST/neondb?sslmode=require
ADMIN_PASSWORD=REPLACE_ME

# Frontend (optional)
# If you deploy the API separately, point frontend to it.
# VITE_API_BASE_URL=https://your-vercel-app.vercel.app
